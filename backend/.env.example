# Conexión a MongoDB Atlas
# Copia este archivo a .env y reemplaza con tu connection string
MONGODB_URI=mongodb+srv://<user>:<password>@cluster.xxxxx.mongodb.net/habitos?retryWrites=true&w=majority

# Puerto del servidor (opcional). Para frontend Semana 2 usar PORT=3001
PORT=5000
